export enum EnumContentType {
  JSON = 'application/json',
  FORM_URLENCODED = 'application/x-www-form-urlencoded',
  FORM_DATA = 'multipart/form-data',
}

export enum EnumStorageKey {
  TOKEN = `wts-token`,
  USERINFO = 'wts-user-info',
}
